name: Android CI

on:
  push:
    branches: [main]
    paths:
      - 'android/**'
      - 'atheer-gpu-shard/**'
      - '.github/workflows/android-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'android/**'
      - 'atheer-gpu-shard/**'
      - '.github/workflows/android-ci.yml'
  workflow_dispatch:
    inputs:
      run_instrumentation_tests:
        description: 'Run instrumentation tests in Android emulator'
        required: true
        default: false
        type: boolean
      emulator_api_level:
        description: 'Android API level for emulator'
        required: true
        default: '34'
        type: choice
        options:
          - '26'
          - '28'
          - '30'
          - '34'
          - '35'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build:
    name: Build Check
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # ── Java & Android SDK ──────────────────────────────────────

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK
        run: |
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --install \
            "ndk;27.0.12077973" \
            --sdk_root="$ANDROID_HOME"

      # ── Rust cross-compilation ──────────────────────────────────

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Restore Rust cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            target/
          key: ${{ runner.os }}-android-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-android-cargo-
            ${{ runner.os }}-cargo-

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      - name: Cross-compile atheer-gpu-shard for arm64-v8a
        run: |
          cargo ndk \
            -t arm64-v8a \
            -o android/atheer-sdk/src/main/jniLibs \
            build -p atheer-gpu-shard --release
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/27.0.12077973

      - name: Verify libatheer_gpu_shard.so was produced
        run: |
          ls -lh android/atheer-sdk/src/main/jniLibs/arm64-v8a/libatheer_gpu_shard.so

      # ── Gradle build ────────────────────────────────────────────

      - name: Restore Gradle cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches/
            ~/.gradle/wrapper/
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/*.gradle.kts', 'android/gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace
        working-directory: android

      - name: Upload APK artifact
        uses: actions/upload-artifact@v6
        with:
          name: atheer-ffi-debug-apk
          path: android/atheer-sdk/build/outputs/apk/debug/atheer-sdk-debug.apk
          retention-days: 7

      - name: Upload GPU shard .so artifact
        uses: actions/upload-artifact@v6
        with:
          name: libatheer_gpu_shard-arm64-v8a
          path: android/atheer-sdk/src/main/jniLibs/arm64-v8a/libatheer_gpu_shard.so
          retention-days: 7

  instrumented-tests:
    name: Instrumentation Tests (Emulator)
    if: >
      github.event_name == 'workflow_dispatch'
      && inputs.run_instrumentation_tests == true
    needs: [build]
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    strategy:
      matrix:
        api-level:
          - ${{ fromJSON(format('[{0}]', inputs.emulator_api_level)) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Download APK artifact
        uses: actions/download-artifact@v6
        with:
          name: atheer-ffi-debug-apk
          path: android/atheer-sdk/build/outputs/apk/debug/

      - name: Download .so artifact
        uses: actions/download-artifact@v6
        with:
          name: libatheer_gpu_shard-arm64-v8a
          path: android/atheer-sdk/src/main/jniLibs/arm64-v8a/

      # ── Emulator ────────────────────────────────────────────────

      - uses: reactivecircus/android-emulator-runner@v2
        id: emulator
        with:
          api-level: ${{ matrix.api-level }}
          arch: arm64-v8a
          target: google_apis
          avd-name: atheer-test
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -memory 2048
          disable-animations: true
          disable-spellchecker: true
          emulator-build: 11076708
          # No script — test execution handled in subsequent steps

      # ── Prepare test models ─────────────────────────────────────

      - name: Push test models to emulator
        run: |
          adb wait-for-device
          adb shell settings put global window_animation_scale 0
          adb shell settings put global transition_animation_scale 0
          adb shell settings put global animator_duration_scale 0

          # GGUFv3 header with tensor_count=0 — invalid model for probe-failure tests
          python3 -c "
          import struct
          header = b'GGUF'
          header += struct.pack('<I', 3)     # version
          header += struct.pack('<Q', 0)     # tensor_count = 0
          header += struct.pack('<Q', 0)     # metadata_kv_count = 0
          with open('/tmp/test_model_corrupt.gguf', 'wb') as f:
              f.write(header)
              f.write(b'\x00' * (4096 - len(header)))
          "
          adb push /tmp/test_model_corrupt.gguf /data/local/tmp/test_model_corrupt.gguf

          # Placeholder for real model test (@Ignore in CI)
          echo "placeholder" > /tmp/test_model.gguf
          adb push /tmp/test_model.gguf /data/local/tmp/test_model.gguf

      # ── Run tests ───────────────────────────────────────────────

      - name: Install APK
        run: adb install -t android/atheer-sdk/build/outputs/apk/debug/atheer-sdk-debug.apk

      - name: Restore Gradle cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches/
            ~/.gradle/wrapper/
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/*.gradle.kts', 'android/gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run connected Android tests
        run: |
          ./gradlew :atheer-sdk:connectedAndroidTest --no-daemon --stacktrace
        working-directory: android
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: android-test-results-api-${{ matrix.api-level }}
          path: |
            android/atheer-sdk/build/reports/androidTests/
            android/atheer-sdk/build/outputs/androidTest-results/
          retention-days: 14
