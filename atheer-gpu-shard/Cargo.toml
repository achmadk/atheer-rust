[package]
name = "atheer-gpu-shard"
version.workspace = true
edition.workspace = true

[lib]
name = "atheer_gpu_shard"
crate-type = ["cdylib"]

[dependencies]
jni = "0.22"
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

[target.'cfg(target_os = "android")'.dependencies]
ash = { workspace = true }
# atheer-accel is referenced only from #[cfg(target_os = "android")] code in lib.rs
atheer-accel = { path = "../atheer-accel" }
